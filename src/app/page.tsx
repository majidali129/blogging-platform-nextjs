import { FeedLayout } from "@/features/feed/components/feed-layout";
export default function HomePage() {
  return <FeedLayout />;
}

/*
feat: comment, bookmark, auth, feed (cruds)
fix: linting & types & layouts
*/
