import { SignUpWithEmailForm } from "@/features/auth/components/sign-up-with-email-form";

const SignUpWithEmailPage = () => {
  return <SignUpWithEmailForm />;
};

export default SignUpWithEmailPage;
